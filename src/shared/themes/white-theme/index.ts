import { createTheme } from "@mui/material";

export const whiteTheme = createTheme({
  palette: {
    primary: {
      main: "#FFFFFF",
      contrastText: "#FFFFFF",
      dark: "#FFFFFF",
    },
  },
});
